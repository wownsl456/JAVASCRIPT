function logParticipant(name){
    console.log(`${name}(이)가 대화에 참여했습니다.`)
  };
  // 코드를 작성해 주세요.
  
  // 테스트 코드
  logParticipant('동수');
  logParticipant('윤하');
  logParticipant('재준');
  logParticipant('동훈');
  logParticipant('영희');
  logParticipant('신욱');
  /* 결과값 = * 동수(이)가 대화에 참여했습니다.
            * 윤하(이)가 대화에 참여했습니다.
            * 재준(이)가 대화에 참여했습니다.
            * 동훈(이)가 대화에 참여했습니다.
            * 영희(이)가 대화에 참여했습니다. 
            * 신욱(이)가 대화에 참여했습니다. */